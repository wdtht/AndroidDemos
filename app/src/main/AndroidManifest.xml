<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.hardware.usb.host"/>


    <application
        android:name=".DemoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidDemos"
        tools:targetApi="31">
        <activity
            android:name=".serialport.SerialportActivity"
            android:exported="false" />
        <activity
            android:name=".customview.activity.DragListActivity"
            android:exported="false" />
        <activity
            android:name=".jetpack.activity.LivedataMainActivity"
            android:exported="false" />
        <activity
            android:name=".network.activity.NetworkActivity"
            android:exported="false" />
        <activity
            android:name=".main.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".customview.activity.CustomViewActivity"
            android:exported="false" />
    </application>

</manifest>