<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:layout_marginBottom="8dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/selector_panel_selection_effect">

    <ImageView
        android:id="@+id/ivIcon"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginLeft="9dp"
        android:visibility="invisible"
        android:src="@drawable/one"
        android:layout_width="18dp"
        android:scaleType="fitXY"
        android:layout_height="15dp" />

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:textColor="@color/color_offset_word"
        android:layout_marginLeft="40dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="标题"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/tvContent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@+id/tvTitle"
        app:layout_constraintRight_toRightOf="@+id/tv_using"
        android:textSize="16sp"
        android:ellipsize="end"
        android:maxLines="1"
        android:layout_marginLeft="22dp"
        android:layout_marginRight="40dp"
        android:textColor="@color/color_offset_word"
        android:text="文本内容"/>

    <TextView
        android:id="@+id/tv_using"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="15dp"
        android:visibility="invisible"
        android:textStyle="bold"
        android:text="Using"
        android:textSize="16sp"
        android:textColor="@color/color_brand"/>


</androidx.constraintlayout.widget.ConstraintLayout>